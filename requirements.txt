python-dotenv
requests
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
networkx>=3.0
PyYAML>=6.0
tqdm>=4.65.0
# LLM relation scorer + query parser. DeepSeek is OpenAI-compatible,
# so we use the openai SDK with base_url=https://api.deepseek.com
openai>=1.30.0
